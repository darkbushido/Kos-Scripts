function free_return_correction {
  local ct to time:seconds + eta:periapsis.
  local data is list(0).
  for step in list(10,1,0.1) {set data to hc["seek"](data, transfit["cor_per_fit"](ct, kerbin, 35000), step).}
  node_exec["exec"](true).
  next().
}
